# Cases — component-renderer-mvp

Traceability: every brief requirement (R1–R7) appears in ≥ 1 case. Checklist items adapted to a local CLI (no auth, no multi-tenancy, no server-side business validation — N/A noted).

**Estado (GATE 2, 2026-09-09):** ✅ implementado + test verde para C01–C16 (ningún ❌). Detalle por caso en `verification.md`. ⚠️ diferido: el event loop interactivo del TUI (teclado/click en terminal) y el mensaje de status en pantalla quedan como verificación manual — YATT no maneja TTYs; toda la lógica subyacente está unit-testeada y el modo headless ejercita la misma orchestration (E2E 24/24 verde).

## Follow-up (owner feedback, 2026-09-09, modo corto)

- **C17 — Explorer tipo árbol (pedido del owner):** las carpetas se desglosan en el lugar (expand/collapse in-place, estilo VS Code), en vez de reemplazar el listado al abrir. Fuente: `pedido` (feedback). Esperado: Enter/→ expande o colapsa un directorio en su posición; ← colapsa o salta al directorio padre; los archivos se seleccionan sin side effects; profundidad visible por indentación. Verificación: unit tests del modelo de árbol (`explorer::nav`) — es TUI, YATT no aplica. Plan: reescritura de `src/explorer/nav.rs` (modelo de filas con set de expandidos + children lazy) + ajuste de render/teclas en `src/explorer/mod.rs`. **Estado: ✅**
- **C18 — Panel de logs (pedido del owner):** panel debajo del explorer que muestra en vivo todo lo que ocurre (npm install, build de ng serve, hot-swaps, errores) con timestamp — porque la instalación tardaba y los errores pasaban en silencio. Fuente: `pedido` (feedback). Verificación: unit tests del `LogSink` (orden, buffer acotado, timestamps) + test TestBackend que assertiona que la cola del sink se renderiza en el panel + errores en rojo; streaming real verificado en smoke headless (stderr espejo). **Estado: ✅**
- **C24 — Versión de Angular + globals del proyecto (owner dumps):** el host pineaba Angular ^20 mientras el workspace real usa ^22 (arrow functions en templates → NG5002) y el `zfill` vive en `prototypes.ts` sin `.d.ts`. Fix: detección de `@angular/core` desde el package.json del proyecto + host cache por major (`host-ng22`) + patch de versiones + `prototypes.ts` puenteado por tipos (tsconfig files) y runtime (`globals.ts` importado estáticamente). Fuente: `descubierto-implementación`. Verificación: suite 58/58 + smoke sintético gym-like renderizando `zfill` en vivo. **Estado: ✅**
- **C22 — Loop infinito de refresh en la página renderizada (owner report):** el stream SSE reenviaba la revisión actual a cada suscriptor nuevo y el host comparaba con `!==` antes de pinear `lastRevision` → cada reconexión disparaba `location.reload()` en bucle. Fix: pinear revisión antes de suscribirse + comparación estrictamente mayor + retry único (sessionStorage) ante fallo transitorio de chunk. Fuente: `descubierto-implementación` (reporte del owner). Verificación: E2E standalone 5/5 + hot-swap 8/8 + probe de estabilidad con marcador de ventana (4s sin reload). **Estado: ✅**
- **C26 — Instalación de deps runtime del proyecto (owner console: `Failed to resolve import "socket.io-parser"`):** el symlink de deps rompía el pre-bundling de Vite (dependencias transitivas pnpm-nested inalcanzables desde el host). Reemplazado por `npm install` de las `dependencies` del proyecto en el host (batch con fallback uno-por-uno para paquetes privados). Fuente: `descubierto-implementación`. Verificación: smoke con `socket.io-client` → `socket.io-parser` presente en el árbol del host + build complete + página renderiza. **Estado: ✅** (limitación D3 visible: 401 del backend real, no bloquea render).
- **C29 — Estilos globales del workspace (owner report: "no carga gym/assets/scss-v2/main.scss"):** puente de `angular.json`: styles globales + `stylePreprocessorOptions.includePaths` del proyecto con longest-prefix match sobre el componente, re-anclados a `./external/...`. Los `@use` de partials compartidos resuelven vía includePaths. Fuente: `pedido` (feedback real). Verificación: unit `bridge_global_styles_maps_nearest_project` + E2E browser (regla global en styleSheets + padding 8px de variable Sass compartida). **Estado: ✅**
- **C31 — Coordinación de recargas + muerte del dev-server (owner: "cae el servidor o no carga"):** el dump (9 swaps, 8 builds OK, 0 fail) probó que el server no cae: la falla percibida son recargas competindo contra rebuilds en curso. Fix: `/api/ready` (pending/ngExited) + el reader de ng marca el settle + la página recarga solo con build asentado + debounce 400ms del watcher + overlay si el dev-server muere. Nota: `core-col` usa sintaxis Sass en estilos compilados como CSS (warning preexistente de SU proyecto). Fuente: `descubierto-implementación` (dump 575 líneas). Verificación: 61/61 + E2E (swap → un solo reload → render). **Estado: ✅**
- **C32 — Datos ficticios tipados para inputs (owner: "auto-rellenalos con datos ficticios según su tipo"):** motor `pipeline_types`: parseo de `input.required<T>/input<T>/model<T>` (scan balanceado de generics), resolución de tipos contra el workspace (interfaces/types/enums/classes, nearest-first, acotado), generación determinística por tipo (strings por semántica del campo, números, booleanos, arrays de 1 elemento, enums al primer miembro, interfaces recursivas con cap de profundidad). Viaja por `/api/current` (`inputValues`) y se aplica con `setInput` en el tick de creación. Defaults del autor se respetan. Fuente: `pedido`. Verificación: `input_values_generated_from_workspace_interfaces` + E2E browser (`Calendar Sample name 05x <user@example.com>`). **Estado: ✅**
- **C30 — Temas scss (owner: `_theme.scss`/`_theme_dark.scss`):** verificado contra el gym real: ambos temas compilan vía `main.scss` puentado (`@use 'themes/theme'/_dark`), light aplica solo (scoped a `body`), dark disponible bajo `body.dark`. Toggle `d` en la página con persistencia localStorage. Fuente: `pedido`. Verificación: styles.css real (415 KB) contiene `body.dark` + `--bg`, toggle en browser light↔dark. **Estado: ✅** (borde D3 observado: componentes con DI de la app quedan vacíos — hito futuro).
- **C28 — Required inputs al montar (owner console: NG0950, dashboard-state):** componentes con `input.required<T>()` explotan al montarse pelados (en la app un padre siempre provee el valor). Fix: extracción de required inputs en el pipeline + `setInput(name, {})` en el host antes del primer CD (placeholder vacío honesto; legacy @Input no cubierto). Fuente: `descubierto-implementación`. Verificación: test de extracción + E2E con fixture `dashboard-state` (renderiza sin NG0950). **Estado: ✅** (inputs quedan como `{}` → bindings vacíos: skeleton honesto, no datos reales).
- **C27 — ERESOLVE por peer conflicts reales (owner dump: monaco-editor vs ngx-monaco-editor-v2):** los workspaces reales conviven con peer conflicts (instalados en modo legacy). Todos los npm install del host ahora usan `--legacy-peer-deps` + el template declara `@angular/compiler-cli` explícitamente (legacy deja de auto-instalar peers). Fuente: `descubierto-implementación`. Verificación: smoke con el conflicto monaco instalado + build complete + render. **Estado: ✅**
- **C25 — Deps del proyecto + host en Angular 22 (owner dump: vite `Failed to resolve import "@angular/forms"`/`ngx-sonner`):** (1) bug de JSON pointer con keys que contienen `/` (`@angular/core` nunca matcheaba → versión siempre default ^20); (2) symlinks de las dependencias runtime del workspace al host `node_modules` (sin pisa-paquetes del host); (3) `baseUrl` deprecado en TS6 → targets `./external/...`; (4) `createNgModule` (ng22). Fuente: `descubierto-implementación`. Verificación: suite 60/60 + smoke ng22 con `zfill` renderizado. **Estado: ✅**
- **C23 — Declaraciones ambientes (`TS2339 zfill`, owner dump):** los prototype patches del proyecto (ej. `String.prototype.zfill`) se declaran en `.d.ts` globales que nadie importa → el host nunca los ve. Fix: puente de `files`/`include` con `*.d.ts` de la cadena de tsconfigs + fallback de barrido acotado (sin node_modules/dist). Además: re-anclaje de `paths` contra el directorio del tsconfig (baseUrl es relativo al config, no a la raíz) y eliminación de early-returns que saltaban etapas del puente. Fuente: `descubierto-implementación` (dump del owner). Verificación: tests `bridge_includes_ambient_declarations_*`, `bridge_globs_dts_*` + smoke gym-like sintético end-to-end (`Application bundle generation complete`). **Estado: ✅**
- **C21 — Panel de logs scrolleable + dump a archivo (pedido del owner):** PgUp/PgDn scrollean el historial completo (Home top / End follow en vivo), `l` vuelca el log completo a `/tmp/render-component-logs.log` para compartirlo. Buffer 1000→5000 líneas, accesores `len`/`range`/`dump_to_file`. Fuente: `pedido` (feedback). Verificación: test TestBackend de scroll (follow vs top) + tests de range/dump/bound. **Estado: ✅**
- **C20 — Puente de tsconfig paths (descubierto en uso real, gym):** los componentes de un workspace Angular real importan vía path aliases (`@lib/*` → `projects/lib/src/*`) que el host no conoce → build falla con "Unknown reference". Fix: `bridge_tsconfig_paths` localiza el tsconfig más cercano caminando desde el componente (hasta 6 niveles, soporta workspaces anidados), sigue la cadena de `extends`, parsea JSONC (comentarios + trailing commas), reescribe los targets con prefijo `external/` (symlink al root del proyecto) y filtra keys de framework (`@angular`, `rxjs`, `tslib`, `zone.js`). Fuente: `descubierto-implementación` (reporte del owner con screenshot de logs). **Rev 2**: el walk-up tenía tope de 6 niveles y el workspace real del owner está a ~13 (`gym/gym/projects/system-modules/...`) → el puente no encontraba nada y TODOS los aliases volvían a fallar (visto en dump `/tmp/render-component-logs.log`). Ahora `find_workspace_config` camina sin tope hasta la raíz explorada y detecta el workspace canónico por `angular.json` (fallback: tsconfig más cercano). Además `strip_ansi` limpia los códigos de color de los streams para que el dump sea legible. Verificación: `find_workspace_config_reaches_deep_nesting_via_angular_json`, `find_workspace_config_falls_back_to_nearest_tsconfig`, `strip_ansi_*` (unit). **Estado: ✅**
- **C19 — Icons en el explorer (pedido del owner; revisión 2 usa `rust-devicons` en lugar de material-icons):** iconos por tipo de archivo vía el crate `devicons` (`icon_for_file`): glyph automático por extensión (ts, tsx, svelte, vue, scss, json, html...) MÁS el color de marca del icono aplicado al span; directorios con glyphs nf-fa-folder/folder-open. Fuente: `pedido` (feedback + revisión). Verificación: test TestBackend que assertiona los glyphs (folder-open + icono TS) junto a los nombres, y parseo de hex → `Color::Rgb`. **Estado: ✅** (requiere Nerd Font en la terminal — sin ella se ven cajas, es solo cosmético).

| ID | Caso | Fuente | Comportamiento esperado | Verificación | Tipo |
| --- | --- | --- | --- | --- | --- |
| C01 | Happy path end-to-end: launch CLI over an Angular sample project, select a standalone component | pedido (R3+R5+R6) | Explorer opens; selecting the component starts a server on a free port; browser shows ONLY that component, styled, with children | YATT test `repro-component-renderer-mvp` → open rendered page URL, assert component visible + its SCSS applied | e2e (pantalla: rendered page) + unit |
| C02 | Explorer navigation: open folders, move selection across files/folders (keyboard; click if supported) | pedido (R3) | Selection moves; folders open/close; selection enters a file without side effects until confirmed | Rust integration test on navigation model + manual/YATT-assisted flow | unit (integration) + e2e |
| C03 | Highlighting of component files by extension (`.component.ts`, `.module.ts` now; `.tsx`, `.svelte`, `.vue` detected) | pedido (R4, R1) | Component files are visually distinct from regular files in the explorer | Unit test on the classifier (path → ComponentKind) + visual assertion in E2E page/screenshot | unit + e2e |
| C04 | Free-port selection | pedido (R5) | Server binds to a free port chosen at runtime (never a hard-coded port); chosen port is reported | Unit test: start server twice concurrently → both succeed on different ports | unit |
| C05 | Renders ONLY that component | pedido (R5) | Page mounts the selected component alone; no other app code/leakage from the host project | YATT: rendered page contains target component selector and NOT sibling markers | e2e |
| C06 | All component imports included: templateUrl, styleUrls/SCSS, child components | pedido (R6) | Rendered output includes template, compiled SCSS, and renders child components | Fixture component with scss + child; unit on pipeline + YATT asserts computed style of child | unit + e2e |
| C07 | Hot-swap: server already up, select a different component | pedido (R7) | Same server process/port keeps serving; view is replaced with the new component; no restart | YATT: select A → assert A; select B → assert B on same URL/port; server pid unchanged | e2e + unit |
| C08 | Non-component file selected | checklist (degenerate input) | No render triggered; selection is inert for serving; only recognized component files arm the render action | Unit test: classifier rejects plain `.ts`, `.html`, etc. | unit |
| C09 | Same component re-selected while already rendered | checklist | No redundant re-render/rebuild; view unchanged | Unit test: idempotent select (no rebuild event) | unit |
| C10 | Broken/unparseable component (syntax error, missing decorator) | checklist (error states) | Early, visible error in explorer/status; server survives; previous view stays or clean error screen | Unit: pipeline returns typed error; YATT: error surfaced, page still reachable | unit + e2e |
| C11 | Race: switch selection while previous compile/render in flight | checklist #5 | Last selection wins; stale compile results discarded/cancelled | Unit test: issue two rapid selects → final state = second component | unit |
| C12 | Missing mandatory source datum (e.g. templateUrl file deleted) | checklist #4 | Early visible feedback (explorer status), never a silent crash only at render time | Unit: resolution error reported before serve | unit |
| C13 | CLI shutdown (Ctrl-C) with server up | checklist #8 (terminal state) | Port released, child processes cleaned, exit code 0 | Integration test: spawn, kill, assert port free again | unit (integration) |
| C14 | Invalid/empty root path passed to CLI | checklist #4 | Early CLI feedback (usage/error), non-zero exit, no explorer | Unit test on arg parsing/validation | unit |
| C15 | NgModule-based component (has sibling `.module.ts`) | descubierto-código (owner's real workspace uses NgModules; pedido lists `.module.ts`) | PENDING GATE 1 (mount with its module / defer) — reserved case, expected behavior written after decision | PENDING decision | unit + e2e |
| C16 | Future-framework files (`.tsx`, `.svelte`, `.vue`) in explorer | pedido (R1, R2) + descubierto | Detected + highlighted; selection behavior PENDING GATE 1 (unsupported message vs hidden) | PENDING decision | unit (+ e2e if visible) |

## N/A checklist items (justified)

- Permisos operador/aprobador/admin, multi-tenancy: local dev CLI, no auth — N/A.
- Server vs UI validation split: applies as "early feedback vs render-time failure" (C10, C12, C14).
- "Ya existe TODO" state: greenfield — degenerate-input equivalents are C08/C09.

## YATT note

YATT drives the **browser**. If the explorer is a terminal TUI (GATE 1 Q1), `e2e` cases assert the rendered page (driven by YATT) while TUI interaction is verified via Rust integration tests + screenshots; if the explorer is a web page, YATT drives the full flow.
